#!/usr/bin/env ruby

require 'securerandom'

# Things we should try to generate:
# - A fixed number of only numbers.
# - A fixed number of only low-bit ascii characters.
# - A string of only characters that aren't confusing.
